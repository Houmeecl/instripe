# Copy to .env and fill in to enable live Stripe checkout.
# When STRIPE_SECRET_KEY is unset, the app runs in self-contained demo mode.
STRIPE_SECRET_KEY=
PORT=3000
CURRENCY=usd
PUBLIC_BASE_URL=http://localhost:3000
